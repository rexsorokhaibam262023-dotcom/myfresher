# ==============================================================================
# MSAP 53rd Freshers' Meet 2026 Ticketing System — Vercel environment template
# ==============================================================================

NODE_ENV="production"

# Public application URL (optional; used for generated pass links)
APP_URL="https://your-project.vercel.app"

# Use a hosted PostgreSQL database (Neon, Vercel Postgres, Supabase, etc.).
# Do not use localhost on Vercel.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
NETLIFY_DATABASE_URL=""
<<<<<<< HEAD
# Require a real database in production. REQUIRE_MYSQL is kept as a legacy alias.
REQUIRE_DATABASE="true"
REQUIRE_MYSQL="false"
=======
REQUIRE_MYSQL="true"
>>>>>>> a06c5a4d5a47dacfd80b29f49a2a494b30b8c7ad
DB_CONNECTION_LIMIT="10"

# Generate unique random values; never commit real secrets.
JWT_SECRET="replace-with-a-long-random-secret"
SESSION_SECRET="replace-with-another-long-random-secret"

# Razorpay Dashboard credentials
RAZORPAY_KEY_ID="rzp_live_replace_me"
RAZORPAY_KEY_SECRET="replace-with-razorpay-key-secret"
# This must be the webhook secret configured in Razorpay Dashboard,
# not the webhook URL.
RAZORPAY_WEBHOOK_SECRET="replace-with-razorpay-webhook-secret"
PAYMENT_UPI_ID="your-verified-vpa@bank"

# Event configuration
EVENT_TICKET_PRICE="350"
EVENT_TIME="5:30 PM Sharp"
EVENT_VENUE="Pune (MSAP Campus Main Auditorium)"

# Admin bootstrap credentials
ADMIN_DEFAULT_EMAIL="admin@msap.org"
ADMIN_DEFAULT_PASSWORD="replace-with-a-strong-password"

# Optional email delivery
EMAIL_API_KEY=""
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
